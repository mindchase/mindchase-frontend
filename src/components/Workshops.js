import React from 'react'
import Card from '@material-ui/core/Card';


const Workshops = () => {

    // this should probably send props to parent at some point :/

    return (
        <>
            
        </>
    )
};

export default Workshops;
